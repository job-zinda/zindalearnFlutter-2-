import 'package:flutter/material.dart';

class AppColors {
  AppColors._(); // Prevents instantiation

  // Your brilliant signature branding accents (Keep these!)
  static const Color primary = Color.fromARGB(255, 125, 5, 245); // Electric Purple
  static const Color secondary = Color(0xFF50E3C2);             // Neon Teal/Mint

  // UNIFIED DARK SURFACES (Tuned to your gorgeous purple background)
  static const Color background = Color(0xFF090123);  // Deep space purple base
  static const Color appBarFill = Color(0xFF0F0433);  // Slightly lighter purple-tinted bar
  static const Color cardFill = Color(0xFF170C47);    // Perfectly blended container layer

  // Core Essentials
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color grey = Colors.grey;
}